# 요구사항 수행 내역서

- **과제명**: 시스템 관제 자동화 스크립트 개발
- **실행 환경**: Docker (ubuntu:24.04 / amd64)
- **실행 바이너리**: agent-app-linux-x86

---

## 1. 수행 내역

### 1-1. SSH 보안 설정

| 항목 | 설정값 | 적용 위치 |
|------|--------|-----------|
| SSH 포트 변경 | `Port 20022` | `/etc/ssh/sshd_config` |
| Root 원격 로그인 차단 | `PermitRootLogin no` | `/etc/ssh/sshd_config` |

```bash
# Dockerfile §9 - 빌드 시 자동 적용
sed -i 's/^#\?Port .*/Port 20022/' /etc/ssh/sshd_config
sed -i 's/^#\?PermitRootLogin .*/PermitRootLogin no/' /etc/ssh/sshd_config
# entrypoint.sh - 컨테이너 기동 시 sshd 실행
/usr/sbin/sshd
```

---

### 1-2. 방화벽(UFW) 설정

| 항목 | 설정값 |
|------|--------|
| 방화벽 종류 | UFW |
| 인바운드 허용 포트 | TCP 20022 (SSH), TCP 15034 (APP) |
| 기본 정책 | incoming deny |

```bash
# entrypoint.sh - 컨테이너 기동 시 적용
ufw --force enable
ufw allow 20022/tcp
ufw allow 15034/tcp
```

---

### 1-3. 계정/그룹 생성

| 계정 | UID | 소속 그룹 | 역할 |
|------|-----|-----------|------|
| agent-admin | 1001 | agent-common, agent-core | 운영·관리·cron 실행 |
| agent-dev | 1002 | agent-common, agent-core | 개발·monitor.sh 소유 |
| agent-test | 1003 | agent-common | QA·테스트 |

| 그룹 | 멤버 |
|------|------|
| agent-common | agent-admin, agent-dev, agent-test |
| agent-core | agent-admin, agent-dev |

```bash
# Dockerfile §3 - 빌드 시 적용
groupadd agent-common
groupadd agent-core
useradd -m -s /bin/bash -u 1001 agent-admin
useradd -m -s /bin/bash -u 1002 agent-dev
useradd -m -s /bin/bash -u 1003 agent-test
usermod -aG agent-common,agent-core agent-admin
usermod -aG agent-common,agent-core agent-dev
usermod -aG agent-common            agent-test
```

---

### 1-4. 디렉토리 구조 및 권한/ACL

| 경로 | 소유자 | 그룹 | 권한 | ACL 기본값 |
|------|--------|------|------|------------|
| `$AGENT_HOME` | agent-admin | agent-admin | 755 | - |
| `$AGENT_HOME/upload_files` | agent-admin | agent-common | 2770 (setgid) | default:group:agent-common:rwx |
| `$AGENT_HOME/api_keys` | agent-admin | agent-core | 2770 (setgid) | default:group:agent-core:rwx |
| `$AGENT_HOME/bin` | agent-admin | agent-admin | 755 | - |
| `/var/log/agent-app` | agent-admin | agent-core | 2770 (setgid) | default:group:agent-core:rwx |
| `$AGENT_HOME/bin/monitor.sh` | agent-dev | agent-core | 750 | - |

```bash
# Dockerfile §4 - 빌드 시 적용
mkdir -p $AGENT_HOME/upload_files $AGENT_HOME/api_keys $AGENT_HOME/bin /var/log/agent-app
chown agent-admin:agent-common $AGENT_HOME/upload_files
chown agent-admin:agent-core   $AGENT_HOME/api_keys
chown agent-admin:agent-core   /var/log/agent-app
chmod 2770 $AGENT_HOME/upload_files $AGENT_HOME/api_keys /var/log/agent-app
setfacl -d -m g:agent-common:rwx $AGENT_HOME/upload_files
setfacl -d -m g:agent-core:rwx   $AGENT_HOME/api_keys
setfacl -d -m g:agent-core:rwx   /var/log/agent-app
# Dockerfile §7 - monitor.sh 권한
chown agent-dev:agent-core $AGENT_HOME/bin/monitor.sh
chmod 750 $AGENT_HOME/bin/monitor.sh
```

---

### 1-5. 애플리케이션 실행 환경

| 환경 변수 | 설정값 |
|-----------|--------|
| AGENT_HOME | `/home/agent-admin/agent-app` |
| AGENT_PORT | `15034` |
| AGENT_UPLOAD_DIR | `/home/agent-admin/agent-app/upload_files` |
| AGENT_KEY_PATH | `/home/agent-admin/agent-app/api_keys` |
| AGENT_LOG_DIR | `/var/log/agent-app` |

키 파일 생성:
```bash
# Dockerfile §5
echo 'agent_api_key_test' > $AGENT_HOME/api_keys/secret.key
chown agent-admin:agent-core $AGENT_HOME/api_keys/secret.key
chmod 660 $AGENT_HOME/api_keys/secret.key
```

앱 실행 (비루트):
```bash
# entrypoint.sh - 컨테이너 기동 시
su agent-admin -c "exec ./agent-app-linux-x86"
```

---

### 1-6. 자동화 스크립트(monitor.sh) 및 cron 등록

| 항목 | 내용 |
|------|------|
| 스크립트 경로 | `$AGENT_HOME/bin/monitor.sh` |
| 실행 언어 | Bash 전용 |
| cron 등록 계정 | agent-admin |
| 실행 주기 | 매분 (`* * * * *`) |
| 로그 경로 | `/var/log/agent-app/monitor.log` |
| 로그 로테이션 | 10MB 초과 시 회전, 최대 10개 보관 |

```bash
# entrypoint.sh - 컨테이너 기동 시
echo "* * * * * $AGENT_HOME/bin/monitor.sh >> $AGENT_LOG_DIR/cron.log 2>&1" \
    | crontab -u agent-admin -
```

---

## 2. 필수 증거 자료 체크리스트

### ✅ 항목 1 — SSH 포트 변경(20022) 및 Root 원격 접속 차단

```
$ grep -E "^(Port|PermitRootLogin)" /etc/ssh/sshd_config
Port 20022
PermitRootLogin no

$ ss -tlnp | grep 20022
LISTEN 0      128          0.0.0.0:20022      0.0.0.0:*    users:(("sshd",pid=9,fd=3))
LISTEN 0      128             [::]:20022         [::]:*    users:(("sshd",pid=9,fd=4))
```

---

### ✅ 항목 2 — 방화벽(UFW) 활성화 및 20022/tcp, 15034/tcp만 허용

```
$ ufw status
Status: active

To                         Action      From
--                         ------      ----
20022/tcp                  ALLOW       Anywhere
15034/tcp                  ALLOW       Anywhere
20022/tcp (v6)             ALLOW       Anywhere (v6)
15034/tcp (v6)             ALLOW       Anywhere (v6)
```

---

### ✅ 항목 3 — 계정/그룹(agent-admin/dev/test, agent-common/core) 생성 확인

```
$ id agent-admin
uid=1001(agent-admin) gid=1003(agent-admin) groups=1003(agent-admin),1001(agent-common),1002(agent-core)

$ id agent-dev
uid=1002(agent-dev) gid=1004(agent-dev) groups=1004(agent-dev),1001(agent-common),1002(agent-core)

$ id agent-test
uid=1003(agent-test) gid=1005(agent-test) groups=1005(agent-test),1001(agent-common)

$ getent group agent-common agent-core
agent-common:x:1001:agent-admin,agent-dev,agent-test
agent-core:x:1002:agent-admin,agent-dev
```

---

### ✅ 항목 4 — 디렉토리 구조 및 권한(ACL 포함) 확인

```
$ ls -ld $AGENT_HOME $AGENT_HOME/upload_files $AGENT_HOME/api_keys $AGENT_HOME/bin /var/log/agent-app
drwxr-xr-x  1 agent-admin agent-admin  4096 /home/agent-admin/agent-app
drwxrws---+ 2 agent-admin agent-common 4096 /home/agent-admin/agent-app/upload_files
drwxrws---+ 1 agent-admin agent-core   4096 /home/agent-admin/agent-app/api_keys
drwxr-xr-x  1 agent-admin agent-admin  4096 /home/agent-admin/agent-app/bin
drwxrws---+ 1 agent-admin agent-core   4096 /var/log/agent-app

$ ls -l $AGENT_HOME/bin/monitor.sh
-rwxr-x--- 1 agent-dev agent-core 4806 /home/agent-admin/agent-app/bin/monitor.sh

$ getfacl $AGENT_HOME/upload_files
# owner: agent-admin
# group: agent-common
# flags: -s-
user::rwx
group::rwx
other::---
default:user::rwx
default:group::rwx
default:group:agent-common:rwx
default:mask::rwx
default:other::---

$ getfacl $AGENT_HOME/api_keys
# owner: agent-admin
# group: agent-core
# flags: -s-
user::rwx
group::rwx
other::---
default:user::rwx
default:group::rwx
default:group:agent-core:rwx
default:mask::rwx
default:other::---
```

---

### ✅ 항목 5 — 앱 Boot Sequence 5단계 [OK] 및 "Agent READY" 확인

```
>> [1/4] SSH 데몬 기동 (포트 20022)
>> [2/4] cron 데몬 기동
>> [3/4] UFW 방화벽 설정 (20022/tcp, 15034/tcp 만 허용)
   UFW 활성화 완료
>> [4/4] agent-admin crontab 등록 (monitor.sh 매분 실행)
>> Boot 준비 완료 - agent-app 실행 (계정: agent-admin)
-------------------------------------------------------------
>>> Starting Agent Boot Sequence...
[1/5] Checking User Account               [OK]
   ... Running as service user 'agent-admin' (uid=1001)
[2/5] Verifying Environment Variables     [OK]
   ... All required Envs correct
[3/5] Checking Required Files             [OK]
   ... Verified 'secret.key' with correct key string.
[4/5] Checking Port Availability          [OK]
   ... Port 15034 is available.
[5/5] Verifying Log Permission            [OK]
   ... Log directory is writable: /var/log/agent-app
------------------------------------------------------------
All Boot Checks Passed!
Agent READY
2026-05-24 08:05:58,037 [INFO] Agent listening at port 15034
```

---

### ✅ 항목 6 — monitor.sh 실행 결과(프로세스/포트/리소스/경고) 확인

```
$ su agent-admin -c /home/agent-admin/agent-app/bin/monitor.sh
====== SYSTEM MONITOR RESULT ======

[HEALTH CHECK]
Checking process 'agent-app-linux-x86'... [OK] (PID: 1)
Checking port 15034... [OK]

[STATUS CHECK]
Firewall (UFW)... [OK] active

[RESOURCE MONITORING]
CPU Usage : 2.9%
MEM Usage : 8.2%
DISK Used : 3%

[INFO] Log appended: /var/log/agent-app/monitor.log
```

---

### ✅ 항목 7 — /var/log/agent-app/monitor.log 누적 기록 확인

```
$ tail -3 /var/log/agent-app/monitor.log
[2026-05-24 08:05:02] PID:1 CPU:1.8% MEM:6.6% DISK_USED:3%
[2026-05-24 08:06:02] PID:1 CPU:3.4% MEM:6.5% DISK_USED:3%
[2026-05-24 08:06:43] PID:1 CPU:2.9% MEM:8.2% DISK_USED:3%
```

로그 형식: `[YYYY-MM-DD HH:MM:SS] PID:... CPU:..% MEM:..% DISK_USED:..%`

---

### ✅ 항목 8 — crontab 매분 실행 등록 및 자동 실행 확인

```
$ crontab -u agent-admin -l
* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/cron.log 2>&1
```

cron 자동 누적 확인 (1분 간격 라인 증가):
```
[2026-05-24 08:05:02] PID:1 CPU:1.8% MEM:6.6% DISK_USED:3%   ← cron 자동 기록
[2026-05-24 08:06:02] PID:1 CPU:3.4% MEM:6.5% DISK_USED:3%   ← 1분 후 자동 추가
```

---

## 3. 비고

- **AGENT_KEY_PATH**: 제공 바이너리가 키 **디렉토리**(`api_keys/`)를 요구하며, 키 파일명은 `secret.key`다. PDF 예시(`t_secret.key`)와 상이하나 실행 바이너리 사양을 따른다.
- **Ubuntu 24.04**: PDF 권장 22.04 대신 사용자 지정 24.04 사용. 기능 동일.
- **보너스 과제(report.sh, 시간 기반 로그 보존)**: 미수행.
